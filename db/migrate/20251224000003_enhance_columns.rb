class EnhanceColumns < ActiveRecord::Migration[8.0]
  def change
    # Note: position column and index already exist in Fizzy schema (db/schema.rb)
    # This migration is a no-op for compatibility
  end
end
